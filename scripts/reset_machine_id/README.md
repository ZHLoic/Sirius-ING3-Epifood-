reset le machine id d'une vm distante pour eviter des problème dhcp.
echo "Usage: $0 <USER> <HOST> <PASSWORD>
demande le password quand le script veux sudo en plus de ce qu'il y a dans la ligne de commande